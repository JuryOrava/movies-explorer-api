{
  "extends": "airbnb-base",
  "rules": {
    "no-underscore-dangle": ["error", { "allow": ["_id"] }],
    "no-unused-vars": ["error", { "argsIgnorePattern": "next" }], // аргемент next в централизованном обработчике ошибок
    "consistent-return": 0
  }
}
